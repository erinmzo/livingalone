import { NextRequest, NextResponse } from "next/server";

export async function GET(request: NextRequest) {
  // 수파베이스에서 개인 프로필을 가져오는 함수를 작성해야함
  return NextResponse.json("");
}

export async function PUT(request: NextRequest) {
  return NextResponse.json("");
}
